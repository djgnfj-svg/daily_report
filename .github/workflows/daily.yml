name: Weekly Report

on:
  schedule:
    # 21:00 UTC Sunday = 06:00 KST Monday
    - cron: '0 21 * * 0'
  workflow_dispatch:
    inputs:
      only_to:
        description: '비워두면 전체 구독자 발송. 이메일 입력 시 그 주소로만 발송 (테스트).'
        required: false
        default: ''

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      issues: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install agent
        working-directory: apps/agent
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run pipeline
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          SITE_URL: ${{ secrets.SITE_URL }}
          PYTHONPATH: apps/agent/src
          ONLY_TO: ${{ inputs.only_to }}
        run: |
          python -c "
          import os
          from datetime import date
          from morningbrief.pipeline.orchestrator import run_for_date
          only_to = os.environ.get('ONLY_TO') or None
          rid = run_for_date(date.today(), send=True, only_to=only_to)
          print(f'report={rid} only_to={only_to}')
          "

      - name: Notify on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              ...context.repo,
              title: `Weekly report failed: ${new Date().toISOString().slice(0, 10)}`,
              body: 'See workflow run logs.',
              labels: ['ops']
            })
